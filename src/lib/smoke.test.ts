import { expect, test } from 'vitest';

// Placeholder so the test harness is proven in CI from Stage 1.
// Stage 3 replaces this with the FDP calculator + table-integrity suites.
test('test harness runs', () => {
  expect(1 + 1).toBe(2);
});
