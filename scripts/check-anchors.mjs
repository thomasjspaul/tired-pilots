// Verifies every legacy `#h.<hash>` anchor id is present in the built HTML.
// Usage: node ./scripts/check-anchors.mjs
//
// STATUS: stub. Implemented in Stage 4 alongside the rehype legacy-anchor plugin.

console.log('[check-anchors] stub — no legacy anchors registered yet');
